Open void_domain/mock_calibration.py.

Update M_surrounding definition to density contrast (Option 1,
Geoffrey Thwaites directive 17 March 2026). Keep all other logic
identical. Groups and void catalogues already loaded.

STEP 1 — Compute cosmic mean density

Using data/tng300_groups_withpos_snap67.csv (28,301 groups):
    total_mass = sum of all mass_200c_1e14Msun values
    box_volume = 205.0^3  # Mpc/h^3
    rho_mean = total_mass / box_volume  # 10^14 Msun / (Mpc/h)^3

Print: "Cosmic mean density <ρ>: {rho_mean:.6f} × 10^14 Msun/(Mpc/h)^3"

STEP 2 — Recompute M_surrounding as density contrast

For each void (from checkpoints/mock_void_table.pkl — reload the
cross-match results, groups per annulus already computed):

    V_annulus = (4π/3) × ((4×R_void)³ − (2×R_void)³)
              = (4π/3) × 56 × R_void³   # Mpc/h^3

    rho_annulus = M_surrounding_raw / V_annulus
                  (M_surrounding_raw = raw summed group mass from
                   previous cross-match, in 10^14 Msun)

    delta = (rho_annulus - rho_mean) / rho_mean

Replace M_surrounding_1e14Msun column with delta in the void table.
Add diagnostic column: rho_annulus, V_annulus_mpc3

Save updated table to: checkpoints/mock_void_table_delta.pkl

Print:
    "delta range: {min:.4f} to {max:.4f}"
    "Median delta: {median:.4f}"
    "Negative delta voids (underdense annuli): {n} / {total}"
    "First 3 rows: {df[['void_id','R_void_mpc','delta']].head(3)}"

STEP 3 — Fit R_void vs delta

Note: delta can be negative. Use two fit approaches:

  Approach A — Linear fit (no log transformation):
    Fit R_void = a × delta + b using scipy linregress
    Record: slope a, intercept b, Pearson r, p-value

  Approach B — Log fit on positive delta only:
    Filter voids where delta > 0
    Fit log10(R_void) = α × log10(delta) + β using scipy ODR
    Record: α, σ_α, Pearson r, reduced χ²/dof
    N used for this fit

STEP 4 — 1000× null shuffle (on delta values)

np.random.seed(20260315)
Shuffle delta labels 1000 times.
Each shuffle: run Approach A linregress, record r_shuffle.
Compute:
    null_r_median = np.median(null_r_dist)
    null_r_95pct  = np.percentile(null_r_dist, 95)
    null_r_99pct  = np.percentile(null_r_dist, 99)

STEP 5 — Print report (raw numbers only — NO pass/fail yet)

    === VOID-DOMAIN v1 Density Contrast Mock Report ===
    Definition:     δ = (ρ_annulus − ⟨ρ⟩) / ⟨ρ⟩
    Cosmic mean ρ:  {rho_mean:.6f} × 10^14 Msun/(Mpc/h)^3
    N voids:        {n_total} total, {n_positive} with δ > 0
    ---
    Approach A (linear, all voids):
      slope a = {a:.4f}, r = {r_linear:.4f}, p = {p:.4e}
    Approach B (log-log, δ > 0 only):
      α = {alpha:.4f} ± {sigma:.4f}, r = {r_log:.4f}
      N used = {n_log}
      χ²/dof = {chi2:.4f}
    ---
    Null shuffle (1000×, linear r):
      Median r:   {null_r_median:.4f}
      95th pct r: {null_r_95pct:.4f}
      99th pct r: {null_r_99pct:.4f}
    ---
    THRESHOLDS: NOT SET — awaiting Grok v1.4 predicted exponent
    ====================================================

IMPORTANT: Do NOT write anything to manifest.py.
Show full output from all five steps.